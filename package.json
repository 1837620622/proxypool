{
  "name": "proxy-pool",
  "version": "1.0.0",
  "description": "A simple proxy pool with liveness check and web interface",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.0",
    "node-cron": "^3.0.0",
    "socks-proxy-agent": "^8.0.0",
    "https-proxy-agent": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
